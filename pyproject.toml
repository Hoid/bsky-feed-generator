[project]
name = ""
version = "0.0.1"
requires-python = ">=3.12"
dependencies = [
    "atproto==0.0.59",
    "peewee~=3.18.2",
    "flask~=2.3.2",
    "python-dotenv~=1.0.0",
]

[dependency-groups]
dev = [
  {include-group = "lint"},
  {include-group = "test"}
]
lint = [
  "ruff"
]
test = [
  "pytest"
]

[tool.uv]
package = false
